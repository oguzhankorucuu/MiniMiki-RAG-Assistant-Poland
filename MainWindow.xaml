<Window x:Class="MiniMiki.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:MiniMiki.Models"
        Title="Mini-MİKİ — E-Ticaret Mevzuat Asistanı"
        Height="720" Width="980"
        Background="{StaticResource BackgroundBrush}">
    <Window.Resources>

        <DataTemplate x:Key="SourceItemTemplate" DataType="{x:Type models:SourceReference}">
            <Button Style="{StaticResource SourceChipStyle}" Click="SourceChip_Click" Tag="{Binding Url}"
                    ToolTip="{Binding Title}" Content="{Binding Id}"/>
        </DataTemplate>

        <DataTemplate x:Key="ChatMessageTemplate" DataType="{x:Type models:ChatMessage}">
            <Border Margin="4,6" Padding="14,10" MaxWidth="680">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="CornerRadius" Value="16,16,16,4"/>
                        <Setter Property="HorizontalAlignment" Value="Left"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Role}" Value="{x:Static models:ChatRole.User}">
                                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="CornerRadius" Value="16,16,4,16"/>
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsError}" Value="True">
                                <Setter Property="Background" Value="{StaticResource ErrorBackgroundBrush}"/>
                                <Setter Property="BorderBrush" Value="{StaticResource ErrorBorderBrush}"/>
                                <Setter Property="BorderThickness" Value="1"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel>
                    <TextBlock Text="{Binding Text}" TextWrapping="Wrap" FontSize="14" LineHeight="20">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Role}" Value="{x:Static models:ChatRole.User}">
                                        <Setter Property="Foreground" Value="{StaticResource TextOnAccentBrush}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsError}" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource ErrorTextBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <StackPanel Margin="0,10,0,0">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Sources.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="KAYNAKÇA" FontWeight="Bold" FontSize="10"
                                   Foreground="{StaticResource TextSecondaryBrush}" Margin="2,0,0,6"/>
                        <ItemsControl ItemsSource="{Binding Sources}" ItemTemplate="{StaticResource SourceItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </StackPanel>
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1" Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Border Width="38" Height="38" CornerRadius="10" Background="{StaticResource AccentBrush}" Margin="0,0,12,0">
                        <TextBlock Text="⚖" FontSize="18" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Mini-MİKİ" FontSize="17" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="E-Ticaret Mevzuat Asistanı" FontSize="11.5" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Ülke" VerticalAlignment="Center" Margin="0,0,10,0"
                               Foreground="{StaticResource TextSecondaryBrush}" FontSize="12.5"/>
                    <ComboBox Width="130"
                              ItemsSource="{Binding AvailableCountries}"
                              SelectedItem="{Binding SelectedCountry}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Sohbet geçmişi -->
        <ScrollViewer Grid.Row="1" x:Name="ChatScrollViewer" VerticalScrollBarVisibility="Auto" Padding="20,16">
            <ItemsControl ItemsSource="{Binding Messages}" ItemTemplate="{StaticResource ChatMessageTemplate}"/>
        </ScrollViewer>

        <!-- Alt bar: yazıyor göstergesi + mesaj kutusu -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0" Padding="20,14">
            <StackPanel>
                <ProgressBar IsIndeterminate="{Binding IsBusy}" Height="2" Margin="0,0,0,10"
                             Background="Transparent" Foreground="{StaticResource AccentBrush}" BorderThickness="0"/>
                <DockPanel>
                    <Button DockPanel.Dock="Right" Content="Gönder" Width="96" Height="38" Margin="10,0,0,0"
                            Command="{Binding SendCommand}"/>
                    <TextBox Text="{Binding QueryText, UpdateSourceTrigger=PropertyChanged}"
                             Height="38" VerticalContentAlignment="Center"/>
                </DockPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
